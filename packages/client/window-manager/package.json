{
  "name": "@panda/window-manager",
  "version": "1.0.0",
  "description": "panda-manager window manager",
  "source": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "watch": "tsc -w -p .",
    "lint": "eslint --quiet --no-error-on-unmatched-pattern --config ../../../.eslintrc.js src/**/*.{ts,tsx,js,jsx}",
    "lint:fix": "eslint --fix --quiet --no-error-on-unmatched-pattern --config ../../../.eslintrc.js src/**/*.{ts,tsx,js,jsx}"
  },
  "dependencies": {
    "@panda/client-utils": "workspace:*",
    "@panda/db": "workspace:*",
    "@panda/shared": "workspace:*",
    "@xiao-ai/utils": "^1.5.1",
    "electron": "17.1.0"
  },
  "devDependencies": {
    "@panda/tsconfig": "workspace:*",
    "@types/node": "^17.0.18",
    "@types/panda": "workspace:*",
    "tslib": "^2.3.1",
    "typescript": "4.6.2"
  }
}
