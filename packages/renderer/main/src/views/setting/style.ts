import { createStyles } from '@panda/renderer-utils';

export const styles = createStyles({
  setting: {
    maxWidth: 680,
    margin: '0 auto',
  },
  select: {
    width: 320,
  },
  input: {
    width: 200,
  },
});
