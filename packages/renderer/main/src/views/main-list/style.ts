import { createStyles } from '@panda/renderer-utils';

export const style = createStyles({
  section: {
    display: 'flex',
    flexDirection: 'column',
    height: '100%',
  },
  body: {
    display: 'flex',
    flexDirection: 'row',
    flexGrow: 1,
  },
});
