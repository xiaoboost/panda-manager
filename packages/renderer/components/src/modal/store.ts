import { styles } from './style';

/** 面板容器 */
export const modalContainer = document.createElement('div');

document.body.appendChild(modalContainer);
modalContainer.setAttribute('class', styles.classes.modalContainer);
