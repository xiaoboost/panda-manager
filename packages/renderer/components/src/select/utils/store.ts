import { styles } from '../style';

/** 下拉列表容器 */
export const dropDownContainer = document.createElement('div');

document.body.appendChild(dropDownContainer);
dropDownContainer.setAttribute('class', styles.classes.dropdownContainer);
