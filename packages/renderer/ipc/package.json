{
  "name": "@panda/renderer-ipc",
  "version": "1.0.0",
  "description": "panda-manager renderer ipc tools",
  "source": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "clear": "rimraf ./dist",
    "build": "pnpm run clear && tsc -p .",
    "lint": "eslint --quiet --no-error-on-unmatched-pattern --config ../../../.eslintrc.js src/**/*.{ts,tsx,js,jsx}",
    "lint:fix": "eslint --fix --quiet --no-error-on-unmatched-pattern --config ../../../.eslintrc.js src/**/*.{ts,tsx,js,jsx}"
  },
  "dependencies": {
    "@types/node": "^17.0.17",
    "electron": "^17.0.0"
  },
  "devDependencies": {
    "@panda/tsconfig": "workspace:*",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  }
}
