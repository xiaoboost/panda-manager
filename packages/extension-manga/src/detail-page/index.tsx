import React from 'react';

import { useParams } from 'react-router';
import { coverPath, previewPath, MangaData } from '../utils';

export function ListCover(data: MangaData) {
    return <div>详情页</div>;
}
