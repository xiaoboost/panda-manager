@import '../../css/variable.styl';

#main-list {
    .main-list-header {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 50px;
        background-color: #3D3D3D;
        box-shadow: 0 3px 3px Shadow;
        padding: 10px;
        display: flex;

        > * {
            flex-grow: 0;
            flex-shrink: 0;
        }
    }
}
