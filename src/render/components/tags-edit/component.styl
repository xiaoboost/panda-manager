#tag-edit-form {
    .ant-form-item-label label {
        display: flex;
        justify-content: flex-end;
        align-items: center;
    }

    .ant-form-item {
        margin-bottom: 18px;

        &:last-child {
            margin-bottom: 0;
        }
    }

    .ant-form-explain {
        position: absolute;
    }
}
