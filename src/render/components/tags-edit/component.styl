#tag-edit-form {
    .ant-form-item-label label {
        display: flex;
        justify-content: flex-end;
        align-items: center;
    }

    .ant-form-item:last-child {
        margin-bottom: 0;
    }
}
